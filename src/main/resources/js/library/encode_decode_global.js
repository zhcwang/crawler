// JavaScript脚本执行引擎全局对象上下文，全局共享

function encodeByCharset(msg, charset) {
	return com.leaning.crawler.js.JavaScriptExecutor.encodeByCharset(msg, charset);
}

function decodeByCharset(msg, charset) {
	return com.leaning.crawler.js.JavaScriptExecutor.decodeByCharset(msg, charset);
}

